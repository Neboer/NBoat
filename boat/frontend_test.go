package boat

import (
	"testing"
)

func TestBindBoatFrontend(t *testing.T) {
	//collection := GetMongoCollection()
	//server := gin.Default()
	//BindBoatFrontend(server.Group(""), collection)
	//_ = server.Run(":8080")
	a := 100/33
	print(a)
}
